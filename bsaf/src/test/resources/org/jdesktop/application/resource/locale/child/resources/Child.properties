# appframework.resource.locale.child.resources.Child.properties

# used to test changing Locale on a ResourceMap chain

#inherited by all local-specific bundles:
import = Foo, Bar

#not shadowed by any other prop files:
Child.default = Child.default

#shadowed by es locale
Child = Child.default
childKey1 = childKey1.default
childKey2 = childKey2.default

#shadows parent properties
key1 = Child.key1.default
key2 = Child.key2.default